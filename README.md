# prestidge-root

Ultra-small static root page for `prestidge.dev`.

## Local preview

```sh
npm install
npm run dev
```

## Deploy

```sh
npm run deploy
```

The Worker is configured as a custom domain for `prestidge.dev` in `wrangler.toml`.
